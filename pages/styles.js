import styled from 'styled-components'

export const Container = styled.div`
    header {
        padding-left: 20px;
        background: var(--background);
        color: var(--blue);
    }
    footer {
        padding-bottom: 20px;
        padding-top: 20px;
        background: var(--background);
        color: var(--blue);
        text-align: center;
    }
    
`;

